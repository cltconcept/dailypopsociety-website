# Chargé par `pnpm dev:api` (node --env-file=server/.env.dev), lancé DEPUIS LA RACINE.
# DATA_DIR et DIST_DIR sont donc relatifs au cwd (la racine), pas à ce fichier.
PORT=4340
DATA_DIR=./data
DIST_DIR=./dist
